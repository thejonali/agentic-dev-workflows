# Keep root-level dotfiles local unless they are explicitly allowed below.
/.*

# Repository metadata that must remain versioned.
!/.gitignore
!/.github/

# Python runtime artifacts.
__pycache__/
*.py[cod]

# Locally generated release artifacts.
/dist/
